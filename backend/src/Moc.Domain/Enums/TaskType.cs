namespace Moc.Domain.Enums;

/// <summary>
/// Categorizes tasks so the UI can group "My Tasks" and reporting can summarize performance.
/// </summary>
public enum TaskType
{
    Evaluation = 1,
    Documentation = 2,
    Approval = 3,
    Implementation = 4,
    Restoration = 5,
    General = 6
}

