namespace Moc.Domain.Enums;

/// <summary>
/// Risk level used for list coloring, KPI grouping, and workflow rules (e.g., auto-add PSSR for yellow/red).
/// </summary>
public enum RiskLevel
{
    Green = 1,
    Yellow = 2,
    Red = 3
}

